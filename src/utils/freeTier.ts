/** Limites do plano free (alinhados ao backend). */
export const FREE_MAX_GROUPS = 10;
export const FREE_MAX_CARDS_PER_GROUP = 20;

/** Temas gratuitos no Pairs (ids dos decks padrão). */
export const FREE_MEMORY_DECK_IDS = new Set(['common-verbs', 'adjectives', 'body-parts']);

/** Karaoke: apenas estas músicas no free. */
export const FREE_KARAOKE_SONG_IDS = new Set(['english-basics-1', 'daily-routines']);

/** Histórias geradas por IA por nível (Readers). */
export const FREE_AI_STORIES_PER_LEVEL = 2;
